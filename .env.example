# Proxy server (Express) - used by the frontend in production, and by Vite proxy in dev
VITE_PROXY_SERVER_URL=http://localhost:3001

# Prediction server (neural network service) - used by the Express proxy
PREDICTION_SERVER_URL=http://localhost:8000

# Express proxy server port
PORT=3001
